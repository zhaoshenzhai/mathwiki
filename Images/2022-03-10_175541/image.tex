\documentclass{article}

\input{../../imageConfig.tex}
\input{../../preamble.sty}

\makeatletter
\newcommand{\globalcolor}[1]{%
    \color{#1}\global\let\default@color\current@color
}
\makeatother

\AtBeginDocument{\globalcolor{textColor}}

\pagenumbering{gobble}

\begin{document}
    \begin{tikzcd}[column sep=3.5em, row sep=2.8em, color=textColor]
        \ms{I}(C) \ar[r, "\ms{I}(\phi)"] \ar[d, "f"'] &
            \ms{I}(C) \ar[d, "f'"] \\
        \Id_\cat{C}(X) \ar[r, "\Id_\cat{C}(\psi)"'] &
            \Id_\cat{C}(X')
    \end{tikzcd}
\end{document}
