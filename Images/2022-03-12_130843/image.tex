\documentclass{article}

\input{../../imageConfig.tex}
\input{../../preamble.sty}

\makeatletter
\newcommand{\globalcolor}[1]{%
    \color{#1}\global\let\default@color\current@color
}
\makeatother

\AtBeginDocument{\globalcolor{textColor}}

\pagenumbering{gobble}

\begin{document}
    \begin{tikzcd}[column sep=3em, row sep =3em, color=textColor]
        X \ar[d, "\alpha"'] &
            \ms{F}(X) \ar[r, "\eta_X"] \ar[d, "\ms{F}(\alpha)"']&
                \ms{G}(X) \ar[d, "\ms{G}(\alpha)"] \\
        Y &
            \ms{F}(Y) \ar[r, "\eta_Y"']&
                \ms{G}(Y)  
    \end{tikzcd}
\end{document}
