\documentclass{article}

\input{../../imageConfig.tex}
\input{../../preamble.sty}

\makeatletter
\newcommand{\globalcolor}[1]{
    \color{#1}\global\let\default@color\current@color
}
\makeatother

\AtBeginDocument{\globalcolor{textColor}}

\pagenumbering{gobble}

\begin{document}
    \begin{tikzcd}[row sep=3em, column sep=3em]
        K^n \ar[d, "L_A"'] &
            V \ar[l, "\phi_\mc{B}"'] \ar[r, "\phi_{\mc{B}'}"] \ar[d, "T" description, background color=bgColor] &
                K^n \ar[d, "L_{A'}"] \ar[ll, "\psi_{\mc{B}'\to\mc{B}}"', bend right] \\
        K^m \ar[rr, "\psi_{\mc{C}\to\mc{C}'}"', bend right] &
            W \ar[l, "\phi_\mc{C}"] \ar[r, "\phi_{\mc{C}'}"'] &
                K^m
    \end{tikzcd}
\end{document}
