\documentclass{article}

\input{../../imageConfig.tex}
\input{../../preamble.sty}

\makeatletter
\newcommand{\globalcolor}[1]{
    \color{#1}\global\let\default@color\current@color
}
\makeatother

\AtBeginDocument{\globalcolor{textColor}}

\pagenumbering{gobble}

\begin{document}
    \begin{tikzcd}[column sep=3em, row sep =3em, color=textColor]
        \ms{F}(X) \ar[r, "\id_{\ms{F}(X)}"] \ar[d, "\ms{F}(\alpha)"']&
            \ms{F}(X) \ar[d, "\ms{F}(\alpha)"] \\
        \ms{F}(Y) \ar[r, "\id_{\ms{F}(Y)}"']&
            \ms{F}(Y)  
    \end{tikzcd}
\end{document}
