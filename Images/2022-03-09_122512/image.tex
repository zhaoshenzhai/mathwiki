\documentclass{article}

\input{../../imageConfig.tex}
\input{../../preamble.sty}

\makeatletter
\newcommand{\globalcolor}[1]{
    \color{#1}\global\let\default@color\current@color
}
\makeatother

\AtBeginDocument{\globalcolor{textColor}}

\pagenumbering{gobble}

\begin{document}
    \begin{tikzcd}[color=textColor]
        \cat{C} \ar[r, "\Id_\cat{C}"] \ar[rr, "\ms{F}\circ\Id_\cat{C}"', bend right] &
            \cat{C} \ar[r, "\ms{F}" description, background color=bgColor]\ar[rr, "\Id_\cat{D}\circ\ms{F}", bend left] &
                \cat{D} \ar[r, "\Id_\cat{D}"'] &
                    \cat{D}
    \end{tikzcd}
\end{document}
