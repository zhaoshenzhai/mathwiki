\documentclass{article}

\input{../../imageConfig.tex}
\input{../../preamble.sty}

\begin{document}
    \begin{tikzpicture}
        \draw (-1.7,0) circle (0);
        \foreach \i in {1,...,100}
        {
            \tikzmath
            {
                \x = 1 - (1 / \i);
            }
            \fill (\x,\x*\x) circle (0.3mm);
        }
        \node at (1,1) [above right] {$x$};
        \draw[dashed] (1,1) circle (6mm);
        \draw[dashed] (1,1) circle (10mm);
    \end{tikzpicture}
\end{document}
