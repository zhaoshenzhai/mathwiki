\documentclass[margin=20mm]{standalone}

\input{../../imageConfig.tex}
\input{../../preamble.sty}

\begin{document}
    \begin{tikzpicture}[color=textColor]
        \pgfmathsetmacro{\scale}{5}
        \node (Pt) at (0,0) {\footnotesize{Partition $\Pi$}};
        \node (Ir) at (\scale,2) {\footnotesize{Induced relation $R_\Pi$}};
        \node (Er) at (2*\scale,0) {\footnotesize{Equivalence relation $R$}};
        \node (Qs) at (\scale,-2) {\footnotesize{Quotient set $X/R$}};
        \path[->]
        (Pt) edge node[sloped, anchor=center, above] {\footnotesize{induces an}} (Ir)
        (Ir) edge node[sloped, anchor=center, above] {\footnotesize{is an}} (Er)
        (Er) edge node[sloped, anchor=center, below] {\footnotesize{induces a}} (Qs)
        (Qs) edge node[sloped, anchor=center, below] {\footnotesize{is a}} (Pt);
        \draw[->] (Ir) to [out=-90, in=0, looseness=1.1] (Pt);
        \draw[->] (Qs) to [out=90, in=0, looseness=1.1] (Pt);
        \draw[->] (Ir) to [out=-90, in=180, looseness=1.2] (Er);
        \draw[->] (Qs) to [out=90, in=180, looseness=1.2] (Er);
        \node[fill=bgColor] at (\scale/2,0) {\footnotesize{$X/R_\Pi$}};
        \node[fill=bgColor] at (3*\scale/2,0) {\footnotesize{$R_{X/R}$}};
    \end{tikzpicture}
\end{document}
